enum CupSize{
    Small, // 0
    Medium, // 1
    Large // 2
}

// Acceso al 'enum key' por valor ('value')
console.log(CupSize[0]); // CupSize (Small)
console.log(CupSize[1]); // CupSize (Medium)
console.log(CupSize[2]); // CupSize (Large)
